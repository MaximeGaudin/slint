# Workspace Clippy policy. `pnpm lint:cli` also passes `-D warnings`.
# Stub macros (todo!, unimplemented!, dbg!) are denied via [workspace.lints.clippy]
# in Cargo.toml. Comment markers (TODO/FIXME/…) are checked by scripts/check-no-todos.mjs.
