{
  "name": "slint-cli",
  "private": true,
  "version": "0.1.0",
  "description": "slint CLI — the linter for Agent Skills.",
  "scripts": {
    "build": "cargo build -p slint-cli --release",
    "test": "cargo test -p slint-cli",
    "coverage": "env CARGO_PROFILE_TEST_DEBUG=0 RUST_TEST_THREADS=1 cargo llvm-cov --workspace --summary-only --ignore-filename-regex='/.cargo/|/rustc/'",
    "lint": "cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && node ../../scripts/check-no-todos.mjs apps/cli/src packages/core/src",
    "format": "cargo fmt --all"
  }
}
