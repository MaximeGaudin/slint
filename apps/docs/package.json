{
  "name": "slint-docs",
  "version": "0.2.0",
  "private": true,
  "description": "The documentation site for slint, built from the linter's own rule catalogue.",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-rules.mjs",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint": "biome check --error-on-warnings . && astro check && node ../../scripts/check-no-todos.mjs apps/docs/src apps/docs/scripts apps/docs/public",
    "format": "biome check --write ."
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.8"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@biomejs/biome": "2.5.8",
    "typescript": "^5.9.3"
  }
}
